const router = require("express").Router();
const api = require("../utils/apiClient");
/**
 * GET  /brands - render page
 * POST /brands - (JSON response if inline new row input )
 * PUT  /brands/:id - update brand (JSON response if inline editing)
 * DELETE /brands/:id
 */
