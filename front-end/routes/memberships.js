const router = require("express").Router();
const api = require("../utils/apiClient");

/**
 * GET /memberships - render memberships page
 * PUT /memberships/:id - update discount percent - JSON inline edit
 */
