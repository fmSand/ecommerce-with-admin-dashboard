const router = require("express").Router();
const api = require("../utils/apiClient");
/**
 * GET/categories - render
 * POST /categories - JSOn if inline new row input
 * PUT /categories/:id- JSON if inline editing
 * DELETE /categories/:id
 */
