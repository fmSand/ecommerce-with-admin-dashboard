<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head', { title: 'Brands' }) %>
<body>
<div class="shell d-flex vh-100 overflow-hidden">
  <%- include('../partials/sidebar', { active: 'brands' }) %>
  <main class="main d-flex flex-column overflow-hidden flex-grow-1">
    <%- include('../partials/topbar', { pageTitle: 'Brands', breadcrumbParent: 'Catalogue' }) %>
    <div class="content flex-grow-1 overflow-y-auto">
      <%- include('../partials/flash') %>
      <header class="d-flex align-items-start justify-content-between mb-4">
        <div>
          <h1 class="page-title h4 fw-bold mb-1">Brands</h1>
          <p class="page-sub mb-0"><%= brands.length %> brand<%= brands.length !== 1 ? 's' : '' %> total</p>
        </div>
        <button type="button" class="btn btn-primary" id="brand-add-btn">
          <i class="bi bi-plus-lg me-1" aria-hidden="true"></i>
          Add Brand
        </button>
      </header>
      <div id="alert-container" class="mb-3" role="status" aria-live="polite" aria-atomic="true"></div>
      <div class="panel">
        <div class="table-toolbar d-flex align-items-center justify-content-between">
          <span class="table-toolbar-title">All Brands</span>
          <span class="table-toolbar-meta"><%= brands.length %> record<%= brands.length !== 1 ? 's' : '' %></span>
        </div>
        <div class="table-responsive">
          <table class="table table-hover mb-0">
            <thead>
              <tr>
                <th scope="col" style="width: 50px;">ID</th>
                <th scope="col">Name</th>
                <th scope="col" class="text-end">Actions</th>
              </tr>
            </thead>
            <tbody>
              <tr id="brand-add-row" class="d-none">
                <td></td>
                <td>
                  <label for="brand-new-name" class="visually-hidden">Brand name</label>
                  <input
                    type="text"
                    id="brand-new-name"
                    class="form-control form-control-sm"
                    placeholder="Brand name"
                    maxlength="100"
                  >
                </td>
                <td class="text-end">
                  <div class="d-flex gap-2 justify-content-end">
                    <button class="btn-action restore" id="brand-save-new-btn" type="button">
                      <i class="bi bi-check-lg me-1" aria-hidden="true"></i>Save
                    </button>
                    <button class="btn-action danger" id="brand-cancel-new-btn" type="button">
                      Cancel
                    </button>
                  </div>
                </td>
              </tr>
              <% if (brands.length === 0) { %>
                <tr>
                  <td colspan="3" class="text-center py-4" style="color: var(--text-3); font-size: 0.82rem;">
                    No brands yet. Click 'Add Brand' to create one.
                  </td>
                </tr>
              <% } %>
              <% brands.forEach(brand => { %>
                <tr data-id="<%= brand.id %>">
                  <td class="text-muted"><%= brand.id %></td>
                  <td>
                    <span class="brand-name fw-medium">
                      <%= brand.name %>
                    </span>
                    <div class="brand-edit-form d-none">
                      <input
                        type="text"
                        class="form-control form-control-sm"
                        value="<%= brand.name %>"
                        maxlength="100"
                        style="max-width: 280px;"
                        aria-label="Edit brand name for <%= brand.name %>"
                      >
                    </div>
                  </td>
                  <td class="text-end">
                    <div class="d-flex gap-2 justify-content-end brand-actions">
                      <button class="btn-action brand-edit-btn" type="button">
                        <i class="bi bi-pencil me-1" aria-hidden="true"></i>Edit
                      </button>
                      <button class="btn-action danger brand-delete-btn" type="button">
                        <i class="bi bi-trash me-1" aria-hidden="true"></i>Delete
                      </button>
                    </div>
                    <div class="brand-edit-actions d-none d-flex gap-2 justify-content-end">
                      <button class="btn-action restore save-btn" type="button">
                        <i class="bi bi-check-lg me-1" aria-hidden="true"></i>Save
                      </button>
                      <button class="btn-action cancel-btn" type="button">
                        Cancel
                      </button>
                    </div>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </main>
</div>
<script src="/js/crud.js"></script>
<script src="/js/brands.js"></script>
<%- include('../partials/scripts') %>
</body>
</html>
